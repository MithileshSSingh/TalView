<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mithilesh.talview">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".mvp.screen_main.MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".mvp.screen_album.AlbumActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".mvp.screen_post.PostActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".mvp.screen_album.FullScreenPhotoActivity"
            android:label="@string/title_activity_full_screen_photo"
            android:theme="@style/AppTheme"></activity>
    </application>

</manifest>